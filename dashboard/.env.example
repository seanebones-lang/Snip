# Copy to .env.local and fill in for local dev
# cp .env.example .env.local

# Backend API base URL (no trailing slash).
# - Local: leave empty to use Vite proxy (localhost:8000).
# - Production: set in Vercel (e.g. https://snip.mothership-ai.com) so the dashboard calls your backend.
VITE_API_URL=
